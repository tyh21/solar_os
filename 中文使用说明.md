---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '67a437b3-5c11-4359-9d3a-70e2c5a5b9e9'
  PropagateID: '67a437b3-5c11-4359-9d3a-70e2c5a5b9e9'
  ReservedCode1: '12efc37a-5aee-4b3b-b74e-ef7060863202'
  ReservedCode2: '12efc37a-5aee-4b3b-b74e-ef7060863202'
---

# SolarOS 中文操作说明

> 适用板卡：**Waveshare ESP32-S3-RLCD-4.2**（SolarOS 官方称为 **SolarTerm**）
> 固件版本：v4.11.0 | ESP-IDF 5.5.4 | 构建日期：2026-09-14

---

## 1. 项目简介

SolarOS 是一个面向"口袋终端"的小型 ESP32 操作系统，主打**文本优先（text-first）**：

- 提供 Shell、前台应用、后台任务（job）
- 内置存储、网络（Wi-Fi / BLE）、硬件服务（显示/按键/音频/传感器/电池）
- 内置 **Python 和 Lua**，用户应用主要用脚本编写
- 构建系统：PlatformIO + ESP-IDF（pioarduino Espressif32 平台）

本项目（solar_term 目标）所基于的硬件即为你的 **Waveshare ESP32-S3-RLCD-4.2**：

| 项目   | 说明                          |
|-------|-------------------------------|
| 显示   | ST7305 反射式 LCD（400×300）   |
| 存储   | SDMMC（SD 卡，已识别 29.8 GB）  |
| 接口   | USB CDC、UART、I2C、SPI       |
| 传感器 | SHTC3 温湿度、电池 ADC          | 
| 音频   | ES8311 / ES7210              |
| RTC   | PCF85063                     |
| 无线   | Wi-Fi + BLE（蓝牙键盘输入）     |

---

## 2. 环境与构建

### 2.1 PlatformIO 位置与 PATH

本项目依赖 PlatformIO。你的机器已安装于：

```sh
export PATH="$HOME/.platformio/penv/bin:$PATH"
```

（已写入 `~/.zshrc`，新开终端即可直接用 `pio`）

### 2.2 关键版本（已踩坑确认）

| 组件               | 版本                            | 说明        |
|-------------------|---------------------------------|------------|
| PlatformIO Core   | ≥ 6.1.19（已升级到 6.2.0）        | 旧版本不兼容 |
| pioarduino 平台    | **55.03.39**（= ESP-IDF 5.5.4） | 本项目 `platformio.ini` 已锁定，**不要**再用 `stable` 链接（stable 会解析到 IDF 5.5.5，导致 NimBLE 补丁哈希校验失败） |

### 2.3 构建与烧录

```sh
# 构建
pio run -e solar_term

# 烧录（板子插 USB 后）
pio run -e solar_term -t upload --upload-port /dev/cu.usbmodemXXXX

# 查看串口日志（注意：非交互终端跑 pio device monitor 会报 termios 错误，建议用 pyserial 脚本抓日志）
```

> 注意：串口设备名每次插入可能变化，以 `/dev/cu.usbmodem*` 实际检测到的为准。
> 板子默认以 **USB JTAG/serial 调试单元**（VID 0x303A）出现在系统里。

---

## 3. 首次启动与交互

### 3.1 启动画面

开机后屏幕显示：`user@sol:/` 提示符 —— 这是 **SolarOS 显示 Shell（display shell）**，说明系统已正常启动。

### 3.2 为什么 USB 串口敲命令没反应？

**这是设计行为，不是故障。**

- 带屏板（如 SolarTerm）：交互 Shell 运行在**屏幕上**，USB CDC **默认不分配 Shell**
- 无屏板（headless）：才会自动把 Shell 放到 UART / CDC 上

所以往 USB 串口发 `help`、回车等命令都没有回显，属正常现象。

### 3.3 输入方式（重要）

这块板子没有板载键盘，SolarOS 的输入方式有：

#### 方式一：BLE 蓝牙键盘（推荐，官方设计）

1. **长按板上的 KEY 键（约 3 秒以上）**，进入 BLE 键盘配对模式（同时清除旧配对）
   - 短按 KEY = 挂起/唤醒（默认 `power key` 为 suspend）
   - 长按 KEY = 蓝牙键盘配对
2. 用你的蓝牙键盘搜索并连接（设备名按固件 BLE 广播显示）
3. 连接后即可在屏幕上输入命令

#### 方式二：让 USB CDC 也有 Shell（可选）

在屏幕 Shell 里执行一次（需要先有输入方式，比如 BLE 键盘）：

```sh
session create shell cdc0
```

之后 USB 串口就会有 Shell 会话，可以像终端一样使用。

---

## 4. 常用命令速查

| 命令                         | 说明                     |
|-----------------------------|------------------------ |
| `help`                      | 命令帮助树                |
| `version`                   | 版本信息                  |
| `commands`                  | 列出所有内置命令           |
| `sessions`                  | 查看当前会话              |
| `session list`              | 同上（会话列表）           |
| `session create shell cdc0` | 在 USB CDC 上开一个 Shell |
| `ble pair`                  | 蓝牙键盘配对               |
| `ble scan`                  | 扫描附近的 BLE 设备        |
| `wifi on/off`               | 开关 Wi-Fi               |
| `ping <host>`               | 网络连通性测试             |
| `power status`              | 电源/睡眠状态              |
| `power suspend`             | 挂起（屏幕关，服务继续跑）   |
| `power sleep`               | 浅睡眠                   |
| `setterm keyrate`           | 按键重复率设置             |
| `pkg`                       | 软件包管理                |
| `board`                     | 查看板卡信息               |
| `battery`                   | 电池电量                  |

---

## 5. 注意事项

1. **不要乱删家目录的 git 仓库**：本机之前构建失败，是因为 `/Users/tianyaohui/.git` 存在一个 SourceTree 遗留的空仓库，CMake 会向上误搜到它。已删除。以后若遇到构建时 `git rev-parse` 相关报错，检查是否有类似残留。
2. **不要用 `stable` 平台链接**：SolarOS 上游使用 `stable`，会解析到新版 IDF（5.5.5），但 SolarOS 的 NimBLE 补丁只支持 5.5.4。本地 `platformio.ini` 已锁定 55.03.39。
3. **默认完整固件**：solar_term 默认使用 full 固件（含 Shell / Python / Lua 等）。

> AI生成

连接wifi：
wifi enable
重启板子
wifi on
wifi scan
wifi connect 你的WiFi名 你的密码


对时
1. NTP 同步时间（WiFi 已连，直接执行）：
ntp
ntp ntp.aliyun.com
ntp cn.ntp.org.cn
默认用 pool.ntp.org，会自动同步时间到 RTC。

2. 设置时区（你在国内，设成 UTC+8）：
setterm timezone UTC+8
同步完成后状态栏的时钟图标就会亮起来并显示时间。


以后直接跑 pio run -e solar_term -t clean 就行。如果需要彻底清理（连下载的依赖包也删），用 pio run -e solar_term -t nobuild 后手动删 .pio 整个目录。